// Single source of truth for generated dependency versions.
// Hardcoded and pinned by contract.

export const DEPENDENCY_VERSIONS = Object.freeze({
    '@zenithbuild/core': '1.0.0',
    '@zenithbuild/router': '1.0.0'
});

export const DEV_DEPENDENCY_VERSIONS = Object.freeze({
    '@zenithbuild/cli': '1.0.0'
});
